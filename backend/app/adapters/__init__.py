"""Source adapters / normalizers.

Populated in **Item 5**: one adapter per provider, mapping that provider's raw
shape into the canonical FDX model behind a common interface. Each adapter is
unit-tested, since taming real-world schema drift is the point.
"""
