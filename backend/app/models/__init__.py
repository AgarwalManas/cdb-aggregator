"""Canonical, FDX-aligned domain model.

Populated in **Item 2**: Account, Balance, Transaction, InvestmentHolding,
Customer, and Consent — modeled on the FDX data model. This is the schema every
data source is normalized into, and the schema the consent layer gates access to.
"""
